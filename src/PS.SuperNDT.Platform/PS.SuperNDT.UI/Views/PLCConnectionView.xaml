<UserControl x:Class="PS.SuperNDT.UI.Views.PLCConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Background="#1F232A">

        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Margin="10"
                Background="#2B313A">
            <TextBlock Text="PLC Connection Management"
                       Margin="15,0"
                       VerticalAlignment="Center"
                       FontSize="22"
                       FontWeight="SemiBold"
                       Foreground="White"/>
        </Border>

        <DataGrid Grid.Row="1"
                  Margin="10"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  ItemsSource="{Binding PlcConnections}"
                  SelectedItem="{Binding SelectedPlc}">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Name"
                                    Binding="{Binding Name}"
                                    Width="180"/>

                <DataGridTextColumn Header="PLC Type"
                                    Binding="{Binding PlcType}"
                                    Width="180"/>

                <DataGridTextColumn Header="IP Address"
                                    Binding="{Binding IpAddress}"
                                    Width="140"/>

                <DataGridTextColumn Header="Rack"
                                    Binding="{Binding Rack}"
                                    Width="70"/>

                <DataGridTextColumn Header="Slot"
                                    Binding="{Binding Slot}"
                                    Width="70"/>

                <DataGridTextColumn Header="Port"
                                    Binding="{Binding Port}"
                                    Width="80"/>

                <DataGridTextColumn Header="Status"
                                    Binding="{Binding Status}"
                                    Width="120"/>

                <DataGridCheckBoxColumn Header="Connected"
                                        Binding="{Binding IsConnected}"
                                        Width="100"/>

                <DataGridTextColumn Header="Read Count"
                                    Binding="{Binding ReadCount}"
                                    Width="100"/>

                <DataGridTextColumn Header="Write Count"
                                    Binding="{Binding WriteCount}"
                                    Width="100"/>

                <DataGridTextColumn Header="Error Count"
                                    Binding="{Binding ErrorCount}"
                                    Width="100"/>

                <DataGridTextColumn Header="Last Heartbeat"
                                    Binding="{Binding LastHeartbeat}"
                                    Width="180"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</UserControl>