<UserControl x:Class="PS.SuperNDT.UI.Views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Background="#1F232A">

        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#2B313A"
                Margin="10">
            <TextBlock Text="Customer Master"
                       Foreground="White"
                       FontSize="22"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       Margin="15,0"/>
        </Border>

        <DataGrid Grid.Row="1"
                  Margin="10"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  ItemsSource="{Binding Customers}"
                  SelectedItem="{Binding SelectedCustomer}">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Code"
                                    Binding="{Binding CustomerCode}"
                                    Width="120"/>

                <DataGridTextColumn Header="Customer"
                                    Binding="{Binding Name}"
                                    Width="250"/>

                <DataGridTextColumn Header="Contact Person"
                                    Binding="{Binding ContactPerson}"
                                    Width="180"/>

                <DataGridTextColumn Header="Mobile"
                                    Binding="{Binding Mobile}"
                                    Width="150"/>

                <DataGridTextColumn Header="Email"
                                    Binding="{Binding Email}"
                                    Width="220"/>

                <DataGridTextColumn Header="GST Number"
                                    Binding="{Binding GstNumber}"
                                    Width="180"/>

                <DataGridCheckBoxColumn Header="Active"
                                        Binding="{Binding IsActive}"
                                        Width="90"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</UserControl>