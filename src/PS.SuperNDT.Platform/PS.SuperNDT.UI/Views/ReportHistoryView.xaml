<UserControl x:Class="PS.SuperNDT.UI.Views.ReportHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1F232A">

    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#252B33"
                CornerRadius="8">

            <TextBlock Text="Report History"
                       Margin="20,0"
                       VerticalAlignment="Center"
                       Foreground="White"
                       FontSize="28"
                       FontWeight="Bold"/>

        </Border>


        <Border Grid.Row="1"
                Margin="0,15,0,0"
                Background="#252B33"
                CornerRadius="8">

            <DataGrid ItemsSource="{Binding History}"
                      SelectedItem="{Binding SelectedHistory}"
                      Margin="10"
                      AutoGenerateColumns="False"
                      IsReadOnly="True">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="Report No."
                                        Binding="{Binding ReportNumber}"
                                        Width="180"/>

                    <DataGridTextColumn Header="Version"
                                        Binding="{Binding Version}"
                                        Width="100"/>

                    <DataGridTextColumn Header="Action"
                                        Binding="{Binding Action}"
                                        Width="150"/>

                    <DataGridTextColumn Header="Description"
                                        Binding="{Binding Description}"
                                        Width="250"/>

                    <DataGridTextColumn Header="Performed By"
                                        Binding="{Binding PerformedBy}"
                                        Width="160"/>

                    <DataGridTextColumn Header="Date"
                                        Binding="{Binding PerformedOn}"
                                        Width="180"/>

                </DataGrid.Columns>

            </DataGrid>

        </Border>

    </Grid>

</UserControl>